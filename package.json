{
  "name": "cloudflare-workspaces-prototype",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@cloudflare/workspace",
    "test": "npm test --workspace=@cloudflare/workspace",
    "build:bin": "npm run build:bin --workspace=@cloudflare/workspace"
  },
  "devDependencies": {
    "@types/node": "^20.14.11",
    "pkg": "^5.8.1",
    "typescript": "^5.5.4"
  }
}
